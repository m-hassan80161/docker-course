version : "3"

services:
  node-app:
    container_name: "testapp1"
    image: muhammed8016/newapp
    build: .
    ports:
     - "4000:4000"
    env_file:
      - .env
    depends_on:
      - db

  web:
    container_name: web
    build:
      context: nginx/.
    ports:
    - "8080:80"
    depends_on:
      - node-app


  db:
    image: postgres
    restart: always
    # set shared memory limit when using docker compose
    shm_size: 128mb
    # or set shared memory limit when deploy via swarm stack
    #volumes:
    #  - type: tmpfs
    #    target: /dev/shm
    #    tmpfs:
    #      size: 134217728 # 128*2^20 bytes = 128Mb
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: example
    volumes:
      - postgres_data:/var/lib/postgresql

  adminer:
    image: adminer
    restart: always
    ports:
      - 8085:8080

  # mongo:
  #   image: mongo:6
  #   restart: always
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: root
  #     MONGO_INITDB_ROOT_PASSWORD: example
  #     MONGO_TCMALLOC_PER_CPU_CACHE_SIZE_BYTES: "0"
  #     GLIBC_TUNABLES: "glibc.pthread.rseq=0"
  #   volumes:
  #     - mongo_db:/data/db

  # mongo-express:
  #   image: mongo-express
  #   restart: always
  #   ports:
  #     - 8081:8081
  #   environment:
  #     ME_CONFIG_MONGODB_URL: mongodb://root:example@mongo:27017/
  #     ME_CONFIG_BASICAUTH_ENABLED: true
  #     ME_CONFIG_BASICAUTH_USERNAME: root
  #     ME_CONFIG_BASICAUTH_PASSWORD: example
  #   depends_on:
  #     - mongo

  redis:
    image: redis

    

volumes:
  mongo_db:
  postgres_data:

  

  # postgres:
  #   image: postgres
  #   restart: always
  #   # set shared memory limit when using docker compose
  #   shm_size: 128mb
  #   # or set shared memory limit when deploy via swarm stack
  #   #volumes:
  #   #  - type: tmpfs
  #   #    target: /dev/shm
  #   #    tmpfs:
  #   #      size: 134217728 # 128*2^20 bytes = 128Mb
  #   environment:
  #     POSTGRES_USER : root
  #     POSTGRES_PASSWORD: example
  #   depends_on:
  #     - node-app
  # adminer:
  #   image: adminer
  #   restart: always
  #   ports:
  #     - 8085:8080


    
    
