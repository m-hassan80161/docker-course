version : "3"

services:
  node-app:
    container_name: "testapp1"
    build:
      context: .
      target: development
    ports:
     - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src:ro
    environment:
     - NODE_ENV=DEVELOPER